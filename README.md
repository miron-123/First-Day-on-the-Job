# First-Day-on-the-Job

Select Distinct C.id;
from floor plan;
where dealer stock;
order by desc;


Select distinct C.id, V.id;
from floor plan;
union all;
select C.id, V.id;
from dealer stock;
as stocklist;
order by;
